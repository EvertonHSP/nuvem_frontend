# URL base da API (backend Flask)
# Use "https://localhost:5000/api" em desenvolvimento local com HTTPS ativado
REACT_APP_API_URL=https://localhost:5000/api

# Chave usada para criptografia no frontend (ex: para proteger dados locais ou requisições sensíveis)
# Substitua por uma chave forte e secreta (não compartilhe sua chave real)
REACT_APP_ENCRYPTION_KEY=your-secure-encryption-key

# Habilita HTTPS no servidor de desenvolvimento do React
# Requer arquivos SSL válidos nos caminhos abaixo
HTTPS=true

# Caminho para o certificado SSL (certificado público)
# Você pode gerar com mkcert, openssl ou usar um certificado autoassinado
SSL_CRT_FILE=./ssl/cert.pem

# Caminho para a chave privada SSL
SSL_KEY_FILE=./ssl/key.pem
